alias Thunderline.Repo

IO.puts("[demo-seeds] Seeding minimal demo data")

# Placeholder: insert baseline communities/channels/users once resources finalized.
# Keep idempotent / safe for repeat nightly resets.

IO.puts("[demo-seeds] (No-op placeholder – extend with real seed logic)")

# PAC Unikernel Walkthrough (MVP)

- Provision from Thunderline → boot unikernel
- Endpoints: GET /health, POST /rpc/run (one Ash action)
- Teardown cleanly; record metrics (start time, req count, p95)

Screens/GIFs to be captured during demo.

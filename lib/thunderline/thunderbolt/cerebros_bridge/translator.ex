defmodule Thunderline.Thunderbolt.CerebrosBridge.Translator do
  @moduledoc "Type-safe boundary for payloads. Encode/decode stubs for future shape mapping."
  @spec encode(term) :: term
  def encode(args), do: args
  @spec decode(term) :: term
  def decode(res), do: res
end

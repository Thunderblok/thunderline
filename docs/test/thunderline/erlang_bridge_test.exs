defmodule Thunderline.ErlangBridgeTest do
  use ExUnit.Case, async: false

  @moduledoc """
  Legacy ErlangBridge test suite disabled; bridge removed.
  Placeholder kept so historical test references don't break tooling.
  """

  @tag :skip
  test "legacy erlang bridge removed" do
    assert :legacy_erlang_bridge_removed
  end

  @tag :skip
  test "legacy thunderbolt operations removed" do
    assert :legacy_erlang_bridge_removed
  end

  @tag :skip
  test "legacy neural pattern conversion removed" do
    assert :legacy_erlang_bridge_removed
  end
end

{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "thunderhelm.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "thunderhelm.labels" . | nindent 4 }}
{{- end }}
